
<form class="form-group" method="post" action="<%= admin_pages_path(public_account_id: @account.id) %>">
  <%= render 'page' %>
</form>
<script>
    const editor = CodeMirror.fromTextArea(document.getElementById("html-template"), {
        lineNumbers: true,
        mode: "html"
    });
    editor.setSize('auto','800px');
</script>