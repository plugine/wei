
<form class="form-group" method="post" action="<%= admin_page_path(@page, public_account_id: @account.id) %>">
  <input type="hidden" name="_method" value="put" />
  <input type="hidden" name="_method" value="put" />
  <%= render 'page' %>
</form>
<script>
    const tabSizeeditor = CodeMirror.fromTextArea(document.getElementById("html-template"), {
        lineNumbers: false,
        mode: "text/html",
        extraKeys: {
            "Esc": function(cm) {
                cm.setOption("fullScreen", !cm.getOption("fullScreen"));
            },
            "Tab": function(cm){
                cm.replaceSelection("  " , "end");
            }
        },
        tabSize: 2,
        indentWithTabs: false
    });
    editor.setSize('auto','800px');
</script>